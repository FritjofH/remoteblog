<div class="row">
  <div class="col-md-8">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <div class="card">
        <div class="card-header">
          <h2>Edit <%= resource_name.to_s.humanize %></h2>
        </div>
        <div class="card-body">
          <div class="mx-3">
            <button class="btn btn-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#accountCollapse" aria-expanded="false" aria-controls="accountCollapse">
              Account information
            </button>
            <button class="btn btn-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#authorCollapse" aria-expanded="false" aria-controls="authorCollapse">
              Author information
            </button>
          </div>
          <%= render "devise/shared/error_messages", resource: resource %>
          <div class="collapse" id="accountCollapse">
            <div class="field row pt-3 mx-3">
              <%= f.label :email, class: 'form-label' %><br />
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
            </div>

            <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
              <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
            <% end %>

            <div class="field row pt-3 mx-3">
              <%= f.label :password, 'New Password', class: 'form-label' %> <i class='form-text'>(leave blank if you don't want to change it)</i><br />
              <%= f.password_field :password, autocomplete: "new-password", class: 'form-control' %>
              <% if @minimum_password_length %>
                <br />
                <em class='form-text' ><%= @minimum_password_length %> characters minimum</em>
              <% end %>
            </div>

            <div class="field row pt-3 mx-3">
              <%= f.label :current_password, class: 'form-label' %> <i class="form-text">(we need your current password to confirm your changes)</i><br />
              <%= f.password_field :current_password, autocomplete: "current-password", class: 'form-control' %>
            </div>
          </div>

          <div class="collapse" id="authorCollapse">
            <%= f.fields_for :author_profile do |author_profile_form| %>
              <div class="field row pt-3 mx-3">
                <%= author_profile_form.label :authorname, class: 'form-label' %><br />
                <%= author_profile_form.text_field :authorname, class: "form-control" %>
              </div>

              <div class="field row pt-3 mx-3">
                <%= author_profile_form.label :biography, class: 'form-label' %><br />
                <%= author_profile_form.text_area :biography, class: "form-control" %>
              </div>
            <% end %>
          </div>

          <div class="field row pt-3 mx-3">
            <%= f.label :password_confirmation, class: 'form-label' %><br />
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control' %>
          </div>
        </div>
        <div class="actions card-footer">
          <%= f.submit "Update", class: 'btn btn-primary' %>
        </div>
      </div>
    </div>
  <% end %>
</div>
