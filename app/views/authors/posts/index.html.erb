<h4 class='d-flex justify-content-between mb-4'>
  <span>Posts</span>
  <div class="d-grid gap-2 d-md-flex justify-content-md-end">
    <%= link_to 'New Post', new_post_path, class: 'btn btn-primary' %>
    <%= button_to "Sign Out", destroy_author_session_path, method: :delete, class: 'btn btn-danger' %>
  </div>
</h4>

<% @posts.each do |post| %>
  <%= link_to edit_post_path(post), class: 'text-decoration-none' do %>
    <div class="card mb-3 post-card">
      <div class="card-body">
        <div class="row">
          <div class="col-md-2">
            <% if post.header_image.present? %>
              <div class="header-img" style="background-image: url(<%= url_for(post.header_image) %>)"></div>
            <% end %>
          </div>
          <div class="col-md-10">
            <% if post.published? %>
              <span class='badge bg-primary mb-3'>Published</span>
            <% else %>
              <span class='badge bg-secondary mb-3'>Unpublished</span>
            <% end %>
            <h5 class='card-title mb-1 text-dark'><%= post.title %></h5>
            <p class='text-secondary mb-0'><%= post.description %></p>
          </div>
        </div>
      </div>
    </div>
    <% end %>
<% end %>